#!/bin/sh

. $HOME/etc/lemonbar/config

while read -r backlight_event; do
    $backlight_event;
    msg="$(xbacklight -get | sed -e 's/\.[0-9]*//g')%"
    msg="%{B$BLACK}%{F$WHITE} $msg %{F-}%{B-}"
    msg="%{F$BLACK}${PL_L}%{F-}$msg"
    echo "$ANCHOR_BACKLIGHT$msg"
done

# vim: set ft=sh:
